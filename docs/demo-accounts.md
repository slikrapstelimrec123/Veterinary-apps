# Demo Account

Use only the fake local account:

- `demo.pet.owner@example.com`

Create the Auth user before running `supabase db reset`. The seed never stores a password and creates only owner-side demo data.

Never use real user, pet, contact, or medical information in demo environments.
